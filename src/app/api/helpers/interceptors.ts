// Request/response interceptors for API
export const requestInterceptor = (config: any) => {
  // Modify request config
  return config;
};

export const responseInterceptor = (response: any) => {
  // Handle response
  return response;
};
